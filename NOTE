int a = 3.2      /***此时 a == 3,并且编译器无报警 ***/
int a = 3 / 2    /***此时 a == 1,并且编译器无报警 ***/
float a = 3 / 2  /***此时 a == 1.000000, 并且编译器无报警***/
float a = 3.0 / 2  /***此时a == 1.500000, 编译器无报警***/

scanf 函数格式串中的空白可以和输入时的多个空白匹配，包括0个，转换说明符前有无空格不重要，但是转换说明符中间的连接符前有无空格很重要：
      scanf("%d/%d", a, b); 和 scanf("  %d/  %d", a, b); 效果一样，在输入时都可以在数字前加不定数量的空格，包括0个；
      但是 scanf("&d/%d", a, b); 和 scanf("%d /%d", a, b); 的效果就不同，如果在输入/前加有空格，函数在读取a的数值后，前者会检测输入的空格和/符号不匹配，此时终止程序。 而后者则可以在输入/之前加上不定数量的空格，包括0个。
      scanf函数中的换行符被当作是空白字符，所以一般不使用，如 scanf("%d  ", a); 当输入a的数值后，敲回车键会被当作空白符，和 %d 后的空白符匹配，直到检测到非空白字符为止才终止程序，scanf("%d\n", a); 和上述格式的效果一样，所以要避免这样。
